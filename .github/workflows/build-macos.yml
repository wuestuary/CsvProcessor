name: Build macOS

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-15  # 升级镜像，自带 Xcode 16
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Select Xcode 16
        run: sudo xcode-select -s /Applications/Xcode_16.app
      
      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
      
      - name: Install MAUI Workloads
        run: |
          dotnet workload install maui
          dotnet workload install maui-maccatalyst
      
      - name: Restore Packages
        run: dotnet restore CsvProcessor.csproj  # 指定项目
      
      - name: Build macOS App
        run: |
          dotnet publish CsvProcessor.csproj \
            -f net9.0-maccatalyst \
            -c Release \
            -p:EnableCodeSigning=false \
            -p:EnablePackageSigning=false \
            -p:CreatePackage=true \
            -p:PublishTrimmed=true \
            -p:TrimMode=partial \
            -p:OutputPath=./output  # 改为项目属性
      
      - name: Zip for Distribution
        run: |
          cd ./output
          zip -r ../CsvProcessor-macOS.zip *.app
      
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CsvProcessor-macOS
          path: ./CsvProcessor-macOS.zip
          retention-days: 30